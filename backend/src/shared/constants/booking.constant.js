const BOOKING_STATUS = Object.freeze({
    PENDING: 'PENDING',
    CONFIRMED: 'CONFIRMED',
    CHECKED_IN: 'CHECKED_IN',
    IN_PROGRESS: 'IN_PROGRESS',
    COMPLETED: 'COMPLETED',
    CANCELED: 'CANCELED',
    NO_SHOW: 'NO_SHOW',
});

const BOOKING_STATUS_VALUES = Object.freeze(Object.values(BOOKING_STATUS));

const BOOKING_ITEM_STATUS = Object.freeze({
    PENDING: 'PENDING',
    IN_PROGRESS: 'IN_PROGRESS',
    PAUSED: 'PAUSED',
    AWAITING_CONFIRMATION: 'AWAITING_CONFIRMATION',
    WAITING_RESOURCE: 'WAITING_RESOURCE',
    DONE: 'DONE',
    SKIPPED: 'SKIPPED',
});

const BOOKING_ITEM_STATUS_VALUES = Object.freeze(Object.values(BOOKING_ITEM_STATUS));

const BOOKING_ITEM_COMPLETION_SOURCE = Object.freeze({
    TIMER: 'TIMER',
    STAFF_EARLY: 'STAFF_EARLY',
    STAFF_CONFIRM: 'STAFF_CONFIRM',
});

const BOOKING_ITEM_COMPLETION_SOURCE_VALUES = Object.freeze(Object.values(BOOKING_ITEM_COMPLETION_SOURCE));

const BOOKING_ARRIVAL_STATUS = Object.freeze({
    EARLY: 'EARLY',
    ON_TIME: 'ON_TIME',
    LATE: 'LATE',
});

const BOOKING_ARRIVAL_STATUS_VALUES = Object.freeze(Object.values(BOOKING_ARRIVAL_STATUS));

const BOOKING_LATE_RESOLUTION = Object.freeze({
    ACCEPT_WITHIN_ORIGINAL_WINDOW: 'ACCEPT_WITHIN_ORIGINAL_WINDOW',
    RESCHEDULED: 'RESCHEDULED',
});

const BOOKING_LATE_RESOLUTION_VALUES = Object.freeze(Object.values(BOOKING_LATE_RESOLUTION));

const BOOKING_HOLD_SLOT_STATUSES = Object.freeze([
    BOOKING_STATUS.PENDING,
    BOOKING_STATUS.CONFIRMED,
    BOOKING_STATUS.CHECKED_IN,
    BOOKING_STATUS.IN_PROGRESS,
]);

const BOOKING_FINAL_STATUSES = Object.freeze([
    BOOKING_STATUS.COMPLETED,
    BOOKING_STATUS.CANCELED,
    BOOKING_STATUS.NO_SHOW,
]);

const BOOKING_CUSTOMER_CANCELABLE_STATUSES = Object.freeze([
    BOOKING_STATUS.PENDING,
    BOOKING_STATUS.CONFIRMED,
]);

const BOOKING_STAFF_CANCELABLE_STATUSES = Object.freeze([
    BOOKING_STATUS.PENDING,
    BOOKING_STATUS.CONFIRMED,
    BOOKING_STATUS.CHECKED_IN,
    BOOKING_STATUS.IN_PROGRESS,
]);

const BOOKING_STAFF_NO_SHOW_STATUSES = Object.freeze([
    BOOKING_STATUS.PENDING,
    BOOKING_STATUS.CONFIRMED,
]);

const BOOKING_PAYMENT_METHOD = Object.freeze({
    CASH: 'CASH',
    PAYOS: 'PAYOS',
});

const BOOKING_PAYMENT_METHOD_VALUES = Object.freeze(Object.values(BOOKING_PAYMENT_METHOD));

const BOOKING_PAYMENT_STATUS = Object.freeze({
    UNPAID: 'UNPAID',
    PENDING: 'PENDING',
    PAID: 'PAID',
    WAIVED: 'WAIVED',
});

const BOOKING_PAYMENT_STATUS_VALUES = Object.freeze(Object.values(BOOKING_PAYMENT_STATUS));

const DEFAULT_BOOKING_RULE = Object.freeze({
    current_tier: 'BRONZE',
    booking_window_days: 7,
    max_upcoming_bookings: 1,
    priority_level: 1,
});

module.exports = {
    BOOKING_STATUS,
    BOOKING_STATUS_VALUES,
    BOOKING_ITEM_STATUS,
    BOOKING_ITEM_STATUS_VALUES,
    BOOKING_ITEM_COMPLETION_SOURCE,
    BOOKING_ITEM_COMPLETION_SOURCE_VALUES,
    BOOKING_ARRIVAL_STATUS,
    BOOKING_ARRIVAL_STATUS_VALUES,
    BOOKING_LATE_RESOLUTION,
    BOOKING_LATE_RESOLUTION_VALUES,
    BOOKING_HOLD_SLOT_STATUSES,
    BOOKING_FINAL_STATUSES,
    BOOKING_CUSTOMER_CANCELABLE_STATUSES,
    BOOKING_STAFF_CANCELABLE_STATUSES,
    BOOKING_STAFF_NO_SHOW_STATUSES,
    BOOKING_PAYMENT_METHOD,
    BOOKING_PAYMENT_METHOD_VALUES,
    BOOKING_PAYMENT_STATUS,
    BOOKING_PAYMENT_STATUS_VALUES,
    DEFAULT_BOOKING_RULE,
};
