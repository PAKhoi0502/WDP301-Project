const WAITLIST_STATUS = Object.freeze({
    WAITING: 'WAITING',
    OFFERED: 'OFFERED',
    ACCEPTED: 'ACCEPTED',
    CANCELED: 'CANCELED',
    EXPIRED: 'EXPIRED',
});

const WAITLIST_STATUS_VALUES = Object.freeze(Object.values(WAITLIST_STATUS));

const WAITLIST_ACTIVE_STATUSES = Object.freeze([
    WAITLIST_STATUS.WAITING,
    WAITLIST_STATUS.OFFERED,
]);

module.exports = {
    WAITLIST_STATUS,
    WAITLIST_STATUS_VALUES,
    WAITLIST_ACTIVE_STATUSES,
};
