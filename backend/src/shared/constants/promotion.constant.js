const PROMOTION_DISCOUNT_TYPES = Object.freeze({
    PERCENTAGE: 'PERCENTAGE',
    FIXED_AMOUNT: 'FIXED_AMOUNT',
});

const PROMOTION_DISCOUNT_TYPE_VALUES = Object.freeze(Object.values(PROMOTION_DISCOUNT_TYPES));

const PROMOTION_AUDIENCES = Object.freeze({
    ALL: 'ALL',
    CUSTOMER: 'CUSTOMER',
    WALK_IN: 'WALK_IN',
});

const PROMOTION_AUDIENCE_VALUES = Object.freeze(Object.values(PROMOTION_AUDIENCES));

const PROMOTION_USAGE_STATUS = Object.freeze({
    RESERVED: 'RESERVED',
    CONSUMED: 'CONSUMED',
    RELEASED: 'RELEASED',
});

const PROMOTION_USAGE_STATUS_VALUES = Object.freeze(Object.values(PROMOTION_USAGE_STATUS));

module.exports = {
    PROMOTION_DISCOUNT_TYPES,
    PROMOTION_DISCOUNT_TYPE_VALUES,
    PROMOTION_AUDIENCES,
    PROMOTION_AUDIENCE_VALUES,
    PROMOTION_USAGE_STATUS,
    PROMOTION_USAGE_STATUS_VALUES,
};
