const PROMOTION_DISCOUNT_TYPES = Object.freeze({
    PERCENTAGE: 'PERCENTAGE',
    FIXED_AMOUNT: 'FIXED_AMOUNT',
});

const PROMOTION_DISCOUNT_TYPE_VALUES = Object.freeze(Object.values(PROMOTION_DISCOUNT_TYPES));

module.exports = {
    PROMOTION_DISCOUNT_TYPES,
    PROMOTION_DISCOUNT_TYPE_VALUES,
};
