const BOOKING_OPERATION_STATUS = Object.freeze({
    NORMAL: 'NORMAL',
    AWAITING_CUSTOMER_DECISION: 'AWAITING_CUSTOMER_DECISION',
});

const BOOKING_OPERATION_STATUS_VALUES = Object.freeze(Object.values(BOOKING_OPERATION_STATUS));

const BOOKING_INCIDENT_TYPES = Object.freeze({
    WASH_BAY_FAILURE: 'WASH_BAY_FAILURE',
    STAFF_UNAVAILABLE: 'STAFF_UNAVAILABLE',
    OTHER_GARAGE_INCIDENT: 'OTHER_GARAGE_INCIDENT',
});

const BOOKING_INCIDENT_TYPE_VALUES = Object.freeze(Object.values(BOOKING_INCIDENT_TYPES));

const BOOKING_INCIDENT_STATUS = Object.freeze({
    AWAITING_CUSTOMER_DECISION: 'AWAITING_CUSTOMER_DECISION',
    RESOLVED: 'RESOLVED',
    VOIDED: 'VOIDED',
});

const BOOKING_INCIDENT_STATUS_VALUES = Object.freeze(Object.values(BOOKING_INCIDENT_STATUS));

const BOOKING_INCIDENT_DECISIONS = Object.freeze({
    REASSIGN_AND_CONTINUE: 'REASSIGN_AND_CONTINUE',
    RESCHEDULE_NEAREST: 'RESCHEDULE_NEAREST',
    RESCHEDULE_CUSTOM: 'RESCHEDULE_CUSTOM',
    CANCEL_BY_GARAGE: 'CANCEL_BY_GARAGE',
});

const BOOKING_INCIDENT_DECISION_VALUES = Object.freeze(Object.values(BOOKING_INCIDENT_DECISIONS));

const BOOKING_INCIDENT_CONTACT_CHANNELS = Object.freeze({
    APP: 'APP',
    PHONE: 'PHONE',
    IN_PERSON: 'IN_PERSON',
});

const BOOKING_INCIDENT_CONTACT_CHANNEL_VALUES = Object.freeze(
    Object.values(BOOKING_INCIDENT_CONTACT_CHANNELS)
);

const BOOKING_INCIDENT_DECISION_SOURCES = Object.freeze({
    CUSTOMER: 'CUSTOMER',
    STAFF_RECORDED: 'STAFF_RECORDED',
});

const BOOKING_INCIDENT_DECISION_SOURCE_VALUES = Object.freeze(
    Object.values(BOOKING_INCIDENT_DECISION_SOURCES)
);

const BOOKING_INCIDENT_CONTINUATION_POLICIES = Object.freeze({
    RESUME_REMAINING: 'RESUME_REMAINING',
    RESTART_CURRENT_ITEM: 'RESTART_CURRENT_ITEM',
});

const BOOKING_INCIDENT_CONTINUATION_POLICY_VALUES = Object.freeze(
    Object.values(BOOKING_INCIDENT_CONTINUATION_POLICIES)
);

const BOOKING_CANCELLATION_SOURCES = Object.freeze({
    CUSTOMER: 'CUSTOMER',
    STAFF_CUSTOMER_REQUEST: 'STAFF_CUSTOMER_REQUEST',
    GARAGE_INCIDENT: 'GARAGE_INCIDENT',
    ADMIN_CORRECTION: 'ADMIN_CORRECTION',
});

const BOOKING_CANCELLATION_SOURCE_VALUES = Object.freeze(Object.values(BOOKING_CANCELLATION_SOURCES));

module.exports = {
    BOOKING_OPERATION_STATUS,
    BOOKING_OPERATION_STATUS_VALUES,
    BOOKING_INCIDENT_TYPES,
    BOOKING_INCIDENT_TYPE_VALUES,
    BOOKING_INCIDENT_STATUS,
    BOOKING_INCIDENT_STATUS_VALUES,
    BOOKING_INCIDENT_DECISIONS,
    BOOKING_INCIDENT_DECISION_VALUES,
    BOOKING_INCIDENT_CONTACT_CHANNELS,
    BOOKING_INCIDENT_CONTACT_CHANNEL_VALUES,
    BOOKING_INCIDENT_DECISION_SOURCES,
    BOOKING_INCIDENT_DECISION_SOURCE_VALUES,
    BOOKING_INCIDENT_CONTINUATION_POLICIES,
    BOOKING_INCIDENT_CONTINUATION_POLICY_VALUES,
    BOOKING_CANCELLATION_SOURCES,
    BOOKING_CANCELLATION_SOURCE_VALUES,
};
