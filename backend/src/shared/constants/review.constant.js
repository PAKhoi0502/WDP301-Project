const REVIEW_MODERATION_STATUSES = Object.freeze({
    PUBLISHED: 'PUBLISHED',
    HIDDEN: 'HIDDEN',
});

const REVIEW_MODERATION_STATUS_VALUES = Object.freeze(
    Object.values(REVIEW_MODERATION_STATUSES)
);

const REVIEW_MODERATION_REASONS = Object.freeze({
    INAPPROPRIATE_LANGUAGE: 'INAPPROPRIATE_LANGUAGE',
    PERSONAL_INFORMATION: 'PERSONAL_INFORMATION',
    SPAM: 'SPAM',
    OFF_TOPIC: 'OFF_TOPIC',
    FRAUD: 'FRAUD',
    OTHER: 'OTHER',
});

const REVIEW_MODERATION_REASON_VALUES = Object.freeze(
    Object.values(REVIEW_MODERATION_REASONS)
);

const REVIEW_SORTS = Object.freeze({
    NEWEST: 'NEWEST',
    OLDEST: 'OLDEST',
    HIGHEST: 'HIGHEST',
    LOWEST: 'LOWEST',
});

const REVIEW_SORT_VALUES = Object.freeze(Object.values(REVIEW_SORTS));

module.exports = {
    REVIEW_MODERATION_STATUSES,
    REVIEW_MODERATION_STATUS_VALUES,
    REVIEW_MODERATION_REASONS,
    REVIEW_MODERATION_REASON_VALUES,
    REVIEW_SORTS,
    REVIEW_SORT_VALUES,
};
