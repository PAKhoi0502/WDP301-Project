const CUSTOMER_VOUCHER_TYPES = Object.freeze({
    FIXED_AMOUNT: 'FIXED_AMOUNT',
    PERCENTAGE: 'PERCENTAGE',
    FREE_SERVICE: 'FREE_SERVICE',
});

const CUSTOMER_VOUCHER_TYPE_VALUES = Object.freeze(Object.values(CUSTOMER_VOUCHER_TYPES));

const CUSTOMER_VOUCHER_STATUS = Object.freeze({
    PENDING_APPROVAL: 'PENDING_APPROVAL',
    ISSUED: 'ISSUED',
    RESERVED: 'RESERVED',
    USED: 'USED',
    EXPIRED: 'EXPIRED',
    REVOKED: 'REVOKED',
});

const CUSTOMER_VOUCHER_STATUS_VALUES = Object.freeze(Object.values(CUSTOMER_VOUCHER_STATUS));

const CUSTOMER_VOUCHER_SOURCES = Object.freeze({
    INCIDENT: 'INCIDENT',
    CUSTOMER_CASE: 'CUSTOMER_CASE',
    ADMIN_GIFT: 'ADMIN_GIFT',
});

const CUSTOMER_VOUCHER_SOURCE_VALUES = Object.freeze(Object.values(CUSTOMER_VOUCHER_SOURCES));

module.exports = {
    CUSTOMER_VOUCHER_TYPES,
    CUSTOMER_VOUCHER_TYPE_VALUES,
    CUSTOMER_VOUCHER_STATUS,
    CUSTOMER_VOUCHER_STATUS_VALUES,
    CUSTOMER_VOUCHER_SOURCES,
    CUSTOMER_VOUCHER_SOURCE_VALUES,
};
