const PLATE_SCAN_STATUSES = Object.freeze({
    CAPTURED: 'CAPTURED',
    QUALITY_REJECTED: 'QUALITY_REJECTED',
    RECOGNIZING: 'RECOGNIZING',
    EXACT_MATCH: 'EXACT_MATCH',
    FUZZY_CANDIDATES: 'FUZZY_CANDIDATES',
    AMBIGUOUS: 'AMBIGUOUS',
    NO_MATCH: 'NO_MATCH',
    MULTIPLE_PLATES: 'MULTIPLE_PLATES',
    ARRIVAL_DETECTED: 'ARRIVAL_DETECTED',
    CONFIRMED: 'CONFIRMED',
    REJECTED: 'REJECTED',
    EXPIRED: 'EXPIRED',
    FAILED: 'FAILED',
});
const PLATE_SCAN_STATUS_VALUES = Object.freeze(Object.values(PLATE_SCAN_STATUSES));

const PLATE_SCAN_MODES = Object.freeze({
    SINGLE: 'SINGLE',
    LIVE_BATCH: 'LIVE_BATCH',
    GATE: 'GATE',
});
const PLATE_SCAN_MODE_VALUES = Object.freeze(Object.values(PLATE_SCAN_MODES));

const PLATE_CAPTURE_SOURCES = Object.freeze({
    STAFF_CAMERA: 'STAFF_CAMERA',
    GALLERY: 'GALLERY',
    LIVE_CAMERA: 'LIVE_CAMERA',
    GATE_CAMERA: 'GATE_CAMERA',
    OFFLINE_GATE: 'OFFLINE_GATE',
});
const PLATE_CAPTURE_SOURCE_VALUES = Object.freeze(Object.values(PLATE_CAPTURE_SOURCES));

const PLATE_MATCH_TYPES = Object.freeze({
    EXACT: 'EXACT',
    FUZZY: 'FUZZY',
    MANUAL: 'MANUAL',
    NONE: 'NONE',
});
const PLATE_MATCH_TYPE_VALUES = Object.freeze(Object.values(PLATE_MATCH_TYPES));

const PLATE_QUALITY_FLAGS = Object.freeze({
    BLUR: 'BLUR',
    GLARE: 'GLARE',
    TOO_DARK: 'TOO_DARK',
    TOO_FAR: 'TOO_FAR',
    BAD_ANGLE: 'BAD_ANGLE',
    PLATE_CROPPED: 'PLATE_CROPPED',
    MULTIPLE_PLATES: 'MULTIPLE_PLATES',
    NO_PLATE_DETECTED: 'NO_PLATE_DETECTED',
    IMAGE_TOO_SMALL: 'IMAGE_TOO_SMALL',
    FILE_TOO_SMALL: 'FILE_TOO_SMALL',
});
const PLATE_QUALITY_FLAG_VALUES = Object.freeze(Object.values(PLATE_QUALITY_FLAGS));

const PLATE_SCAN_REJECTION_REASONS = Object.freeze({
    VEHICLE_MISMATCH: 'VEHICLE_MISMATCH',
    WRONG_BOOKING: 'WRONG_BOOKING',
    POOR_IMAGE: 'POOR_IMAGE',
    CUSTOMER_NOT_PRESENT: 'CUSTOMER_NOT_PRESENT',
    DUPLICATE_SCAN: 'DUPLICATE_SCAN',
    OTHER: 'OTHER',
});
const PLATE_SCAN_REJECTION_REASON_VALUES = Object.freeze(Object.values(PLATE_SCAN_REJECTION_REASONS));

const ALTERNATE_VEHICLE_STATUSES = Object.freeze({
    NONE: 'NONE',
    REQUESTED: 'REQUESTED',
    APPROVED: 'APPROVED',
    REJECTED: 'REJECTED',
});
const ALTERNATE_VEHICLE_STATUS_VALUES = Object.freeze(Object.values(ALTERNATE_VEHICLE_STATUSES));

const CAMERA_DEVICE_STATUSES = Object.freeze({
    ACTIVE: 'ACTIVE',
    MAINTENANCE: 'MAINTENANCE',
    INACTIVE: 'INACTIVE',
    REVOKED: 'REVOKED',
});
const CAMERA_DEVICE_STATUS_VALUES = Object.freeze(Object.values(CAMERA_DEVICE_STATUSES));

const CAMERA_HEALTH_STATUSES = Object.freeze({
    ONLINE: 'ONLINE',
    STALE: 'STALE',
    OFFLINE: 'OFFLINE',
    DISABLED: 'DISABLED',
});

const normalizeLicensePlate = (value) => (
    typeof value === 'string' ? value.toUpperCase().replace(/[^A-Z0-9]/g, '') : ''
);

module.exports = {
    PLATE_SCAN_STATUSES,
    PLATE_SCAN_STATUS_VALUES,
    PLATE_SCAN_MODES,
    PLATE_SCAN_MODE_VALUES,
    PLATE_CAPTURE_SOURCES,
    PLATE_CAPTURE_SOURCE_VALUES,
    PLATE_MATCH_TYPES,
    PLATE_MATCH_TYPE_VALUES,
    PLATE_QUALITY_FLAGS,
    PLATE_QUALITY_FLAG_VALUES,
    PLATE_SCAN_REJECTION_REASONS,
    PLATE_SCAN_REJECTION_REASON_VALUES,
    ALTERNATE_VEHICLE_STATUSES,
    ALTERNATE_VEHICLE_STATUS_VALUES,
    CAMERA_DEVICE_STATUSES,
    CAMERA_DEVICE_STATUS_VALUES,
    CAMERA_HEALTH_STATUSES,
    normalizeLicensePlate,
};
