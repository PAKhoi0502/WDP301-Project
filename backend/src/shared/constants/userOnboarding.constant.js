const USER_ONBOARDING_STATUSES = Object.freeze({
    ACTIVE: 'ACTIVE',
    PENDING_PASSWORD_SETUP: 'PENDING_PASSWORD_SETUP',
    PENDING_PHONE_VERIFICATION: 'PENDING_PHONE_VERIFICATION',
});

const USER_ONBOARDING_STATUS_VALUES = Object.freeze(
    Object.values(USER_ONBOARDING_STATUSES)
);

module.exports = {
    USER_ONBOARDING_STATUSES,
    USER_ONBOARDING_STATUS_VALUES,
};
