const VEHICLE_INSPECTION_TYPES = Object.freeze({
    BEFORE_WASH: 'BEFORE_WASH',
    AFTER_WASH: 'AFTER_WASH',
});

const VEHICLE_INSPECTION_TYPE_VALUES = Object.freeze(Object.values(VEHICLE_INSPECTION_TYPES));

module.exports = {
    VEHICLE_INSPECTION_TYPES,
    VEHICLE_INSPECTION_TYPE_VALUES,
};
