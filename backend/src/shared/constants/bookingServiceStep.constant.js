const BOOKING_SERVICE_STEP_STATUS = Object.freeze({
    PENDING: 'PENDING',
    IN_PROGRESS: 'IN_PROGRESS',
    DONE: 'DONE',
    SKIPPED: 'SKIPPED',
});

const BOOKING_SERVICE_STEP_STATUS_VALUES = Object.freeze(Object.values(BOOKING_SERVICE_STEP_STATUS));

const BOOKING_SERVICE_STEP_WORKFLOW_TYPES = Object.freeze({
    PRE_SERVICE: 'PRE_SERVICE',
    SERVICE: 'SERVICE',
    POST_SERVICE: 'POST_SERVICE',
});

const BOOKING_SERVICE_STEP_WORKFLOW_TYPE_VALUES = Object.freeze(Object.values(BOOKING_SERVICE_STEP_WORKFLOW_TYPES));

const BOOKING_SERVICE_STEP_CODES = Object.freeze({
    PRE_SERVICE_CHECK_IN: 'PRE_SERVICE_CHECK_IN',
    POST_SERVICE_HANDOVER: 'POST_SERVICE_HANDOVER',
});

module.exports = {
    BOOKING_SERVICE_STEP_STATUS,
    BOOKING_SERVICE_STEP_STATUS_VALUES,
    BOOKING_SERVICE_STEP_WORKFLOW_TYPES,
    BOOKING_SERVICE_STEP_WORKFLOW_TYPE_VALUES,
    BOOKING_SERVICE_STEP_CODES,
};
